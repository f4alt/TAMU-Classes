#include <bits/stdc++.h>

using namespace std;

int main() {
  int row_amt, col_amt, temp;

  cin >> row_amt >> col_amt;

  while (row_amt--) {
    cin >> temp;
  }
  while (col_amt--) {
    cin >> temp;
  }

  return 0;
}
